---
echo "set ts=2 sts=2 sw=2 et nu ai cuc cul" > ~/.vimrc

export do="--dry-run=client -o yaml"
export now="--force --grace-period=0" 

cut/copy/paste (d (delete/cut) /y (yank) /p (paste)) u undo

set number
set tabstop=2
set expandtab
set shiftwidth=2
set cursorline
   

 